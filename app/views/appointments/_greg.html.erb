<div class="row">
<h1>Here are your upcoming sessions</h1>
<ol>
	<% if @appointments.length >= 1 %>
<% @appointments.each do |t| %>
	<li><%= t.hour.strftime('%A') %>, <%= t.date %> At <%= t.hour.strftime('%I %p') %></li>
	<% end %>
	<% end %>
</ol>
</div>